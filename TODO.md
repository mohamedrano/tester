كمال ما تبقى من TODO.md بتسلسل الأولويات (تنفيذ فوري، بلا أعذار)

قواعد قسرية عامة (تنطبق على كل البنود دون استثناء):

حواجز قبول صارمة: تغطية الاختبارات ≥ 95% للمشروع ككل، أخطاء Lint = 0، فشل واحد يُسقط المهمة بالكامل.

الحتمية: اختبارات حتمية ببذور ثابتة، لا Sleep عشوائي، لا «TODO/LATER». أي تخطٍ أو تأجيل يُعد فشلاً.

الأداء: زمن تنفيذ الاختبارات ≤ 5 دقائق محليًا (p95) مع تشغيل متوازٍ.

المخرجات: تقارير HTML موحّدة تحت ./reports/، مع أثر تغييرات واضح (diff) لكل تعديل.

الدمج: يُحظر الدمج إلى الفرع الرئيس ما لم تمرّ جميع البوابات أدناه.

1) UnitTestingAgent — P0 الآن

نفّذ: إنشاء هيكل اختبارات وحدات تحت tests/unit/**, وتمكين توليد وتشغيل الاختبارات تلقائيًا. دمج fast-check (Property-Based) وts-mockito (Mocks/Stubs) داخل محرّك الاختبارات.
مخرجات إلزامية:

ملفات: tests/unit/**/*.test.ts, tests/setup/unit.setup.ts.

Property tests لطبقات المنطق الخالص والUtilities الحرجة.

تغطية وحدة ≥ 97% لملفات المنطق الأساسية.
بوابة قبول: فشل أي Property أو غياب Mocks الصحيحة ⇒ فشل المهمة.

2) IntegrationTestingAgent — P0

نفّذ: محاكاة API والبيانات باستخدام MSW مع سيناريوهات تكامل تشمل طبقة الشبكة والمخازن (stores) وتنسيق الأخطاء.
مخرجات إلزامية:

ملفات: tests/integration/msw/handlers.ts, tests/integration/**/*.itest.ts, tests/setup/msw.setup.ts.

سيناريوهات نجاح/إخفاق/Timeout/Retriable.
بوابة قبول: أي استدعاء شبكة غير مُعترض بـ MSW أو منطق إعادة المحاولة غير مُختبَر ⇒ فشل.

3) E2ETestingAgent — P0

نفّذ: توليد اختبارات مستخدم شاملة عبر Playwright مع cross-browser (Chromium/Firefox/WebKit).
مخرجات إلزامية:

ملفات: tests/e2e/**/*.e2e.ts, playwright.config.ts.

تغطية المسارات الحرجة (التشغيل البارد، المصادقة، التدفق الرئيس من البداية للنهاية).
بوابة قبول: أقل من 90% من المسارات الحرجة مُغطاة أو تعطيل متصفّح واحد ⇒ فشل.

4) SecurityAgent — P0

نفّذ: فحوص الثغرات والمصادقة ودمج أدوات ZAP/Snyk/npm audit ضمن خطّة محلية وCI.
مخرجات إلزامية:

تقارير: ./reports/security/zap.html, ./reports/security/snyk.json, مخرجات npm audit محفوظة.

اختبارات مصادقة سلبية/إيجابية (جلسات، رموز، CSRF/SSRFi حيث ينطبق).
بوابة قبول: أي ثغرة High/Critical غير مُعلّمة «Known-Accepted» مع تبرير وخطّة معالجة مؤرّخة ⇒ فشل.

5) PerformanceAgent — P0

نفّذ: سيناريوهات حمل/ضغط باستخدام k6 أو Artillery ومراقبة أداء نقاط النهاية والمسارات UI الحرجة.
مخرجات إلزامية:

ملفات: perf/scenarios/*.js|ts, تقارير تحت ./reports/perf/.

عتبات: p95 زمن الاستجابة للخدمات الأساسية ≤ 300ms (قابلة للتعديل بمبرّر مكتوب).
بوابة قبول: تجاوز العتبات دون «Exemption» موقّع ومؤرّخ ⇒ فشل.

6) MaintenanceAgent — P0

نفّذ: كشف الاختبارات الهشّة (Flaky) وإصلاحها، مع تحديث تلقائي للاعتماديّات ضمن قيود SemVer وتحليل أثر تغييرات الاختبارات.
مخرجات إلزامية:

تقارير Flakiness مع إعادة تشغيل تلقائي محدود ووسم @flaky مؤقّت مع مذكّرة إصلاح.

سيناريو «Affected Tests Only» لتسريع الدورة، وتقرير أثر (Impact) لكل ترقية.
بوابة قبول: أي اختبار «Flaky» غير مُعالج خلال دورتين تشغيل متتاليتين ⇒ فشل.

7) ReportingAgent — P0

نفّذ: تجميع نتائج جميع الوكلاء في لوحة/تقرير تفاعلي HTML/Allure موحّد، يربط الأمن/الأداء/التغطية/الـE2E.
مخرجات إلزامية:

./reports/allure/** + صفحة فهرس ./reports/index.html بروابط الأقسام.

شريط مؤشرات: Coverage، Lint، Perf p95/p99، Security Findings، Flaky Count.
بوابة قبول: أي قسم مفقود أو روابط مكسورة ⇒ فشل.

8) Orchestrator App — P0 ختامي

نفّذ: ربط كل الوكلاء عبر broker داخلي (pub/sub أو queue) وتنفيذ سير عمل واحد:
Unit → Integration → E2E → Security → Perf → Maintenance → Reporting
مع توقّف مبكّر عند أول فشل وتوليد «خلاصة فشل» تشخّص السبب.
مخرجات إلزامية:

ملف تهيئة orchestrator ومسارات المهام، وسجل تنفيذ مرتب زمنيًا تحت ./reports/logs/orchestrator.log.
بوابة قبول: السماح بالاستمرار رغم فشل بوابة سابقة ⇒ فشل.

بوابات الدمج الإلزامية (Merge Gates)

اجتياز جميع البوابات أعلاه + توليد ./reports/index.html.

عدم اجتياز أي بوابة ⇒ يُمنع الدمج ويُرفع تقرير الفشل مع الأثر والخطّة.

التعليمات الأخيرة

نفّذ الآن بتسلسل الأولويات كما ورد أعلاه.

عند الانتهاء: أنشئ تعليق خلاصة يتضمّن الروابط النسبية لكل تقرير، نسب المؤشرات، وقائمة الاستثناءات المبرّرة زمنيًا.

أي محاولة تجاوز/تأجيل/تخفيف من غير مذكّرة موقّعة ومؤرّخة تُعد فشلاً نهائيًا.
